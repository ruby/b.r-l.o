<% if User.current.allowed_to?(:edit_tags, @projects || @project) %>
  <li><%= context_menu_link sprite_icon('tag-plus', l(:label_tags_add_tags), plugin: :redmineup_tags),
                            edit_issue_tags_path(:ids => @issue_ids),
                            :remote => true,
                            :class => 'icon icon-add-tags' %></li>
<% end %>
