ruby ENV['CUSTOM_RUBY_VERSION'] || '~> 3.2.2'

gem 'puma'
gem 'good_job'
gem 'pg'
gem 'rack-protection'

group :development do
  gem 'pry-rails'
  gem 'dotenv-rails'
end

group :production do
  gem 'newrelic_rpm'
  gem 'airbrake'
end
